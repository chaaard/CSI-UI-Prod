import { Box } from '@mui/material';
import { useEffect } from 'react';

const AcctGCash = () => {
  useEffect(() => {
    document.title = 'Accounting | GCash';
  }, []);

  return (
    <Box>
    </Box>
  )
}

export default AcctGCash